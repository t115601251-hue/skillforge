请你把这个素材包整合进项目 README。

具体要求：
1. 将 `assets/readme/` 文件夹放在仓库根目录。
2. 修改 README.md，在对应章节插入图片。
3. 使用相对路径，例如：`assets/readme/01-hero.png`。
4. 不要使用绝对路径，不要 base64，不要删除原 README 的主体内容。
5. 插图位置：
   - 标题、badges、一句话说明下方插入 `01-hero.png`
   - “闭环流程”章节插入 `02-workflow.png`
   - “五种核心场景”章节插入 `03-core-scenarios.png`
   - “12 个 slash 命令”章节插入 `04-commands.png`
   - “MECE 5+1 分类”章节插入 `05-mece-categories.png`
   - “安全设计”章节插入 `06-security.png`
   - `07-github-insert-guide.png` 作为维护说明图，可放在 README 最后或 docs 中
6. 修改完成后，请检查 GitHub Markdown 预览中所有图片是否能正常显示。
